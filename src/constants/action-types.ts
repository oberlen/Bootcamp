type AT = {
  //UI actions
  SET_BTN: boolean;
  //Fetch middleware actions
  GET_OVERVIEW_ITEMS: undefined;

  //helloMessage action set
  SET_HELLO: string;

  //helloMessage action get
  GET_HELLO:string;
};

export default AT;
